/// Configuration for map hosting
/// Maps are now bundled as assets in the Flutter app
class MapConfig {
  // No external hosting needed - maps are included in assets
  static const bool useBundledAssets = true;
}
